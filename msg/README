this folder is just meant to be run as follows:

git clone --depth=1 https://github.com/simwijs/naoqi_bridge_msgs -b ros2
mv naoqi_bridge_msgs ../../ && cd ../../../
colcon build && source install/setup.zsh
rm -rf src/naoqi_bridge_msgs